---
"konekt": minor
---

`Provider` now exposes the WalletConnect Cloud project ID it was configured with as a readonly `projectId` property, so UI layers can reuse it without carrying the value separately.
